# 퍼스널 브랜딩 사이트 — 작업로그 (2026-08-04)

> 이전 로그: [2026-08-03](포폴사이트_작업로그_2026-08-03.md) (실측 좌표·인터랙션 사양) · [2026-07-31](포폴사이트_작업로그_2026-07-31.md) (워크플로 기초)
> 이 문서는 **HOW I WORK 섹션 신설 · 기여도 카드 · 챗봇 · 무대 여백 도입** 기록.

---

## 1. 클라이언트 피드백 대응 (이번 작업의 큰 축)

**"좌우 여백이 없어 답답하다"** → 무대 전체를 축소해 여백을 만드는 방식으로 해결.

- `js/main.js` 의 `STAGE_MARGIN = 0.04` (화면 폭의 4%, 최소 24 / 최대 120px)
- `sf = (innerWidth - margin*2) / 1920` 로 계산하고 `--stage-margin` 을 CSS 변수로 내려보냄
- 세 개의 스케일 래퍼(`.stage`, `.values-scale`, `.footer-scale`)가 `translateX(var(--stage-margin)) scale(var(--sf))`

**왜 이 방법인가:** `--gutter` 를 키우는 방식은 하드코딩 좌표 53개를 전부 옮겨야 하고
(좌측 기준은 +, 우측 기준은 − 로 방향이 달라 실수 위험이 큼), Figma 시안과 좌표가 어긋나
이후 실측 워크플로가 깨진다. 무대 축소는 **한 줄이면서 정렬을 100% 보존**한다.

⚠️ **여백 도입 시 같이 보정해야 하는 곳** (놓치면 어긋남):
- 헤더 패딩 → `calc(var(--stage-margin) + var(--pad-x) * var(--sf))`
- 히어로 영상 시작 좌표 → `margin + BOX.x * sf`
- 푸터 다크 배경 → `.site-footer` 가 아니라 **`.footer-reveal`** 에 칠해야 화면 끝까지 참

**부작용:** 전체가 6~7% 작아져 12px 라벨이 화면상 10px 아래로 내려감.
여백을 더 키우라는 요구가 오면 **작은 글자만 1~2px 올려 보정**할 것.

---

## 2. 신설 — 03-b. HOW I WORK 섹션

가치 섹션과 프로젝트 01 사이. 버튼으로 넘기는 카드 4장 (가로 스크롤 강제 없음).

- 카드는 같은 자리에 겹쳐두고 `.is-active` 만 표시. 끝에서 순환. `<button>` 이라 키보드 조작 가능
- 시안 실측: 영문 100px·행간 0.9·자간 -3 / 한글 24px·행간 1.3 / 라벨·카운터 16px·자간 -0.48 / 이미지 213×264
- **영문은 80px 로 낮춤** — 100px 에서 "One problem, defined sharply." 가 두 줄로 넘어감
- **이미지는 가로 4:3** (work-section-01~04) 이라 프레임을 360×270 으로 바꾸고 본문 칼럼을 600 으로 이동
- 내용(프로세스 4단계)은 자리표시. 문구·이미지 확정되면 교체

### 섹션 라벨 공통화
`HOW I WORK` 과 `FEATURED PROJECTS` 가 같은 규칙을 쓴다 (`.story-label, .cta-label`).
- General Sans 500 · 16px · 자간 -0.48 + **CSS 로 그린 마름모**(11px 정사각 45° 회전)
- ⚠️ **마크업에 `◆` 문자를 넣지 말 것** — CSS 마커와 겹쳐 두 개로 보임 (실제로 겪음)

---

## 3. 신설 — 기여도 카드 (`.meta-card`)

프로젝트 01·02·03 의 점선 자리표시를 실제 카드로 교체. 417×514 공통.

- 구성: 라벨 → **기술 스택(26px, 카드에서 가장 큼)** → 단계별 막대
- 막대는 **수치를 노출하지 않고** 최대값을 100% 로 둔 상대 길이 (`style="--v:70%"`)
- 결과보고서의 참여도 수치는 *팀 내 지분이 아니라* 본인 작업 시간의 단계별 분포였음
  (합이 98%, 103%) → 개인 프로젝트에도 같은 의미로 쓸 수 있어 GOI 는 라벨만 `SOLO PROJECT` 로

### ⚠️ 장표만 보고 적었다가 실제와 달랐던 건
| 항목 | 장표 | 실제 코드 |
|---|---|---|
| W:RUN | jQuery, Figma MCP | **React 19 + TypeScript, Tailwind 4, Claude API** (jQuery 없음) |
| GOI | — | React 19 + Vite, PWA, html2canvas, **AI 연동은 미구현** |

**교훈: 기술 스택은 반드시 레포의 package.json 으로 검증할 것.** 면접에서 검증당하는 자료다.

---

## 4. 신설 — AI 챗봇

W:RUN(`ezen_teamproject_2`)의 구조를 그대로 가져와 포트폴리오용으로 옮김.

```
js/chat.js  →  POST /api/chat  →  api/chat.js (Vercel edge)  →  Anthropic API
                                        ↑ api/knowledge.js
```

- **API 키는 서버 환경변수(`ANTHROPIC_API_KEY`)에만.** 프론트는 자체 엔드포인트만 호출
- 방어 장치(원본에서 계승): IP당 분당 15회, 최근 20개 메시지만, 메시지당 2000자, 응답 1024토큰
- 모델 기본값 `claude-haiku-4-5-20251001` (환경변수 `ANTHROPIC_MODEL` 로 교체 가능)
- **프롬프트 캐싱** 적용 — 시스템 프롬프트 + 지식이 매 요청 동일하므로 `cache_control: ephemeral`
- 응답에 `model` 을 함께 반환 → 첫 인사말 아래 "현재 ○○ 버전을 사용 중입니다" 문구가 자동 정정

### 말투
면접관이 열람한다는 전제를 프롬프트 맨 앞에 명시. **하십시오체만**, 감탄사·이모지·느낌표 금지,
농담·역할극 요청 사양. 화면 고정 문구(인사말·오류·유도 칩)도 같은 격식으로 통일.

### `api/knowledge.js`
프로젝트 3건의 지식을 분리 보관. **`(작성 필요)` 항목은 지어내지 말고 직접 문의를 안내**하도록 규칙화.
- 채워진 것: 기간·구성·역할·단계별 비중·기술 스택·구현 특징
- 비어 있는 것: **주요 의사결정과 근거 / 어려웠던 지점 / 결과·배운 점** (전 프로젝트)
  GOI 추가: 왜 만들었나 / 왜 순수 JS → React 재구축인가 / 왜 PWA인가 / 상점·친구 기획 의도
- ⚠️ 작업 비중은 순위로만 답하게 함 (16.67% 같은 수치를 챗봇이 읊지 않도록)

⚠️ **Live Server 에서는 동작하지 않음** — `/api/chat` 이 없어 404. `vercel dev` 또는 배포에서만.

---

## 5. 신설 — 플로팅 버튼 묶음 (`.fab-stack`)

흰 알약 하나에 3개를 세로로. 우 40 / 하 60, 너비 100px, 버튼 84×56.

| 버튼 | 동작 |
|---|---|
| ⌃ | 맨 위로 (모션 최소화 설정이면 즉시 이동) |
| InApp | https://card-news-editor-peach.vercel.app/ (새 탭) |
| 챗봇 | 패널 토글 |

챗봇 아이콘은 W:RUN 의 `header-chatbot.svg` 를 **CSS 마스크**로 사용 —
원본이 라임색으로 칠해진 도형이라 그냥 넣으면 색을 못 바꾼다. 마스크로 쓰면 `currentColor` 를
따라가 호버 시 흰색으로 반전된다.

---

## 6. 푸터 등장 방식 변경 (reveal)

본문이 위로 걷히면서 아래 깔린 푸터가 드러나는 방식.

- 푸터를 무대 밖으로 빼 `position: fixed; bottom: 0` (z0), 본문 `.stage-wrap` 은 z1 + **불투명 배경**
- `.footer-space` (= 푸터 높이) 만큼 스크롤 여유를 두어 정확히 다 드러남
- `#contact` 앵커를 `.footer-space` 로 옮김 — **고정된 요소는 스크롤 목적지가 될 수 없음**

⚠️ **3D 오브제를 되살릴 때 충돌한다.** 이 효과는 본문이 불투명해야 성립하는데,
현재 `#hero3d` 캔버스는 `.stage-wrap` 뒤에 있어 가려진다. 되살리려면 캔버스를
`.stage-wrap` **안쪽 맨 뒤**로 옮겨야 한다.

---

## 7. 타이포·간격 조정

| 대상 | 이전 | 지금 | 이유 |
|---|---|---|---|
| 인트로 한글 카피 | 50px | **24px** | 한글이 크고 길어 답답 |
| 인트로 영문 헤드라인 | — | **56px 신설** | 큰 영문 + 작은 한글 위계 |
| 헤드라인↔카피 간격 | 40px | **20px** | 버튼 간격(49)과 대비를 만들어 한 덩어리로 |
| 가치 섹션 소카피 | 24px | **20px** | 한글 본문 축소 흐름에 맞춤 |
| `.hero-studio` top | 779 | **861** | DELIVER 하단(611+250)에 문단 상단을 맞춤 |

`--fs-50` 은 인트로 영문이 물려받았다가 `--fs-56` 으로 교체(→ `--fs-50` 제거).
`--fs-80` 신설(HOW I WORK 영문).

---

## 8. 인터랙션 조정

**인트로 시퀀스** — 3D 오브제 비활성에 맞춰 대기 시간 제거. 전체 3.6초 → **2.2초**
`0.0s 헤더 → 0.7s THINK(+스튜디오·화살표·십자) → 1.0s MAKE &(+영상 등장·재생) → 1.3s DELIVER`
⚠️ 3D 를 되살리면 `--delay` 들을 hero3d.js 종료 시각(약 1.6s) 뒤로 다시 밀 것.

**프로젝트 부유 효과** — "여백이 좁아 답답하다"는 피드백에 등장 시점을 늦춤
- `FLOAT_START 0.85` : 요소 상단이 화면 높이의 85% 지점에 와야 **비로소 나타나기 시작** (그전엔 투명)
- `FLOAT_SETTLE 0.20` : 이 지점에서 제자리 안착
- `FLOAT_FADE 0.6` : 진행률 60% 에서 페이드인 완료
- 이징은 easeInOutCubic — `pow(1-p,3)` 은 움직임의 87%가 전반에 몰려 "너무 빨리 도착"

---

## 9. 남은 일

1. **`api/knowledge.js` 의 `(작성 필요)` 채우기** ← 챗봇 품질에 가장 큰 영향
2. **Vercel 환경변수 `ANTHROPIC_API_KEY` 등록 후 재배포** (환경변수는 기존 배포에 소급 적용 안 됨)
   키는 `ezen_teamproject_2/.env` 에 있음. 콘솔에서는 재발급만 가능
3. HOW I WORK 카드 문구·이미지 확정
4. GOI 기여도 막대 수치 확정 (현재 추정치), `17 Screens` 표기 검토 (라우트 17 / 페이지 컴포넌트 16)
5. 프로젝트 02·03 툴 라인 외 리스트 섹션 빈 공간 정리 (제목 130 · 화살표 1790 사이가 늘어져 보임)
6. 히어로 영상 파일 삽입 (`#heroVideo .hero-video-inner` 안에 `<video muted loop playsinline>`)
7. 서브페이지 5개 — 공통 헤더/푸터는 JS include 로 중복 제거 예정 (리액트 불필요 결론 유지)
8. 배포 전 `loading="lazy"` 적용 검토 (메인에 이미지가 계속 늘어나는 중)
